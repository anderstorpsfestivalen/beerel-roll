CREATE TABLE "inventory" (
    name TEXT,
    product_number INTEGER PRIMARY KEY,
    volume INTEGER,
    orderable BOOLEAN,
    consumed BOOLEAN,
    consumed_by TEXT,
    consumed_time DATETIME,
    image_url TEXT,
    rejected TEXT
);